# Decisions

Use this file to preserve system-level conventions and important changes.

Example:
- Google Drive is the primary memory layer
- GitHub is the source of truth for code state
- Librarian and local `librarian_memory/` folders are AI-managed by default
- Users should express intent, not manually rewrite memory files unless needed
